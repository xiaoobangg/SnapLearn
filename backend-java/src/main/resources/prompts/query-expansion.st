你是信息检索与查询扩展专家。请基于原始查询生成 {number} 个**语义不同但意图一致**的查询变体，以扩大检索召回面。

每个变体应该从不同角度切入，例如：
- 换用同义词 / 近义词
- 改换表达视角（"如何 X" → "X 的步骤"、"X 的方法"）
- 补充隐含的限定词或上下文
- 拆分或合并原查询中的概念

严格要求：
1. 输出语言必须与原始查询一致（中文查询就输出中文变体，英文查询就输出英文变体）。
2. 只输出查询变体本身，每个一行，**严格输出 {number} 行**。
3. 不要编号（不要写"1." "2."）、不要前缀（不要写"变体："）、不要解释、不要空行、不要引号。
4. 每个变体必须是独立可检索的完整查询，不能依赖其他变体。
5. 不要重复原始查询。

原始查询：{query}

查询变体：
