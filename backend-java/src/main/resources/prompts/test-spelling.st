为单词"{word}"（释义：{meaning}）生成一道"拼写组合"题。将单词拆分成多个字母片段，并把正确的片段打乱后和干扰片段混在一起。

要求：
1. options：正确片段（2-3个）+ 干扰片段（2-3个），共4-5个片段，随机排列，不要按正确顺序排列
2. answer：正确片段按顺序拼接后的完整单词（即"{word}"）
3. question："根据释义拼写单词：{meaning}"

返回JSON示例：
{{"question":"根据释义拼写单词：好的","options":["go","od","ni","goo","do"],"answer":"good"}}

注意：
- 正确片段拼接后必须等于原单词，干扰片段应看起来像原单词的组成部分但不属于正确拼写
- options 中正确片段不能按拼接顺序排列，必须随机打乱
